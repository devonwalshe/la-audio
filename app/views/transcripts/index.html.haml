%p#notice= notice
%h1 Listing Transcripts
%table
  %thead
    %tr
      %th Body
      %th Audio
      %th{colspan: "3"}
  %tbody
    - @transcripts.each do |transcript|
      %tr
        %td= transcript.body
        %td= transcript.audio_id
        %td= link_to 'Show', transcript
        %td= link_to 'Edit', edit_transcript_path(transcript)
        %td= link_to 'Destroy', transcript, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Transcript', new_transcript_path
